{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_admin_panel'|trans }}
{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-3"></div>
        <div class="col-6">
            <div class="d-flex justify-content-center my-5">
                <a href="{{ url('user_list') }}" class="btn btn-success">
                    {{ 'title_user_list'|trans }}
                </a>
            </div>
            <div class="d-flex justify-content-center my-5">
                <a href="{{ url('reservation_list') }}" class="btn btn-success">
                    {{ 'title_reservation_list'|trans }}
                </a>
            </div>
            <div class="d-flex justify-content-center my-5">
                <a href="{{ url('category_index') }}" class="btn btn-success">
                    {{ 'title_category_list'|trans }}
                </a>
            </div>
            <div class="d-flex justify-content-center my-5">
                <a href="{{ url('tag_index') }}" class="btn btn-success">
                    {{ 'title_tag_list'|trans }}
                </a>
            </div>
            <div class="d-flex justify-content-center my-5">
                <a href="{{ url('author_index') }}" class="btn btn-success">
                    {{ 'title_author_list'|trans }}
                </a>
            </div>
        </div>
        <div class="col-3"></div>
    </div>


{% endblock %}