{% include 'default/_form.html.twig' with {
    page_title: 'title.element_edit'|trans({'%id%': element.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'element_index',
} %}