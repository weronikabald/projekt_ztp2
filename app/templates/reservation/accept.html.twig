{% include 'default/_form.html.twig' with {
    page_title: 'title.reservation_accept'|trans({'%id%': reservation.id|default('')}),
    form: form,
    submit_label: 'action.accept'|trans,
    back_to_list_path: 'reservation_index',
} %}